import { DoesContainPipe } from './does-contain.pipe';

describe('DoesContainPipe', () => {
  it('create an instance', () => {
    const pipe = new DoesContainPipe();
    expect(pipe).toBeTruthy();
  });
});
