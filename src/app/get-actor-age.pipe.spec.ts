import { GetActorAgePipe } from './get-actor-age.pipe';

describe('GetActorAgePipe', () => {
  it('create an instance', () => {
    const pipe = new GetActorAgePipe();
    expect(pipe).toBeTruthy();
  });
});
